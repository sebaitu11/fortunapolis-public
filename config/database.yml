# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  database: auction_development
  encoding: utf8
  username: sebastian
  password:
  host: localhost

production:
  adapter: postgresql
  database: auction_development
  encoding: utf8
  username: sebastian
  password:
  host: localhost

test:
  adapter: postgresql
  database: auction_test
  encoding: utf8
  username: sebastian
  password:
  host: localhost
